export default (page, pageSize, searchQuery, sortBy, orderBy, minPrice, maxPrice, minDuration, maxDuration) => ({
    page,
    pageSize,
    searchQuery,
    sortBy,
    orderBy,
    minPrice,
    maxPrice,
    minDuration,
    maxDuration
})